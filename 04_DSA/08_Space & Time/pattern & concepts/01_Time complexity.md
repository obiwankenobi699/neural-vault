

The Time complexity is the function which tells us how time is grow as input is grows
what does fn = O(g(n)) do 

example a graph of two different devices one may take large time to execute another may take less time but both graph is linearly growing as both of them having same time complexity 


1we always go for worst case in time complexity 
2 time complexity only applicable for large dataset
example

$$
f(N) = O(g(N))
$$
$$
lim = f(N)/g(N) < infinity
N->infinity
$$

$$

6N^3 + 3N+5

6/1+3/infinity+5/infinity 
6<infinity
$$



3 we remove constants
example y = 3x+4 where after solving this eq we get time taken but but the time complexity 

So in this equation after solving we get the time taken to execute the code 
but what we want is the function so we dont consider the constants

and function is same in every device 

4 always ignore less dominating term 

big O :-it give upper bound of our algo but it may not we taking worst case 
theta
omega
Little notations