# 🚢 **THE ULTIMATE LINUX STORAGE OCEAN VOYAGE** 🚢
*A Complete Maritime Guide to Disks, Filesystems, and Bootloaders*

```
                    🌊 WELCOME TO STORAGE SEAS 🌊
         
              ___     ___     ___
            _|:::|_ _|:::|_ _|:::|_    
         __/::::::\::::::\:::::::\    LINUX STORAGE MASTERY
        /   \:::::/     \/     \/      - Disk Commands
        |  S |:::|   H   |  I  |       - Partitioning
        |  T |:::|   D   |  P  |       - Filesystems  
        |  O |:::|   D   |  S  |       - Bootloaders
        |  R |:::|   !   |  !  |       - Swap Magic
        |  A |:::|       |     |       
        |  G |:::|_______|_____|       Captain: Claude
        |  E |::::::::::::::::/        Vessel: SS Arch Linux
         \__/::::::::::::::/          
            ~~~~~~~~~~~~~~~~~
```

---

# 📦 **PART 1: EVERY DISK & SPACE COMMAND**

## 🗺️ **MAP 1: DISK DISCOVERY COMMANDS**

```bash
┌─────────────────────────────────────────────────────────┐
│         🔍 DISK RECONNAISSANCE FLEET 🔍                 │
└─────────────────────────────────────────────────────────┘

# ⚓ BASIC RECON
lsblk                    # List all block devices (tree view)
lsblk -f                 # Add filesystem info
lsblk -o NAME,SIZE,TYPE,MOUNTPOINT,FSTYPE,UUID

fdisk -l                 # List all disks + partitions (detailed)
sudo fdisk -l /dev/sda   # Specific disk

parted -l                # GNU parted listing
sudo parted /dev/sda print

blkid                    # Show UUIDs + filesystem types
sudo blkid               # More complete info

# 🧭 DISK DETAILS
hdparm -I /dev/sda       # Hard drive parameters
smartctl -a /dev/sda     # SMART disk health (install smartmontools)
lshw -class disk         # Hardware lister for disks

# 🏴‍☠️ PARTITION TABLE TYPE
sudo parted /dev/sda print | grep 'Partition Table'
lsblk -d -o NAME,PTTYPE  # MBR or GPT?
```

---

## 📊 **MAP 2: SPACE USAGE COMMANDS**

```bash
┌─────────────────────────────────────────────────────────┐
│         💾 CARGO HOLD INSPECTION 💾                     │
└─────────────────────────────────────────────────────────┘

# 🌊 FILESYSTEM SPACE
df                       # Disk free (all filesystems)
df -h                    # Human readable (GB/MB)
df -h /                  # Just root filesystem
df -h /home              # Just /home
df -i                    # Inode usage (file count)
df -T                    # Show filesystem type

# 🗂️ DIRECTORY SPACE
du -sh /home/*           # Size of each dir in /home
du -sh /*                # Size of root level dirs
du -h --max-depth=1 /    # One level deep
du -ah /var | sort -hr | head -20  # Top 20 largest

# 🎯 INTERACTIVE SPACE EXPLORERS
ncdu /                   # NCurses Disk Usage (best!)
ncdu -x /                # Stay on one filesystem
baobab                   # GNOME Disk Usage Analyzer (GUI)

# 🔍 FILE HUNTERS
find / -type f -size +1G 2>/dev/null        # Files over 1GB
find /home -type f -size +500M -exec ls -lh {} \;
locate -S                # Database statistics

# 📈 REAL-TIME MONITORING
watch -n 5 'df -h'       # Update every 5 seconds
```

```
         ASCII DIAGRAM: DISK vs FILESYSTEM SPACE
         
    PHYSICAL DISK (500GB)
    ═══════════════════════════════════════════
    ║  /dev/sda                               ║
    ╠═══════════════════════════════════════════
    ║  Partition 1: /boot (1GB)               ║
    ║  ┌─────────────────────────────┐        ║
    ║  │ Used: 200MB │ Free: 800MB   │        ║
    ║  └─────────────────────────────┘        ║
    ╠═══════════════════════════════════════════
    ║  Partition 2: / (50GB)                  ║
    ║  ┌─────────────────────────────┐        ║
    ║  │█████████████░░░░░ 48GB/50GB │ ← FULL!║
    ║  └─────────────────────────────┘        ║
    ╠═══════════════════════════════════════════
    ║  Partition 3: /home (449GB)             ║
    ║  ┌─────────────────────────────┐        ║
    ║  │██░░░░░░░░░░░░░ 100GB/449GB  │        ║
    ║  └─────────────────────────────┘        ║
    ╚═══════════════════════════════════════════
```

---

# ⚙️ **PART 2: INCREASE/DECREASE PARTITION SIZE**

## 🔧 **METHOD 1: NON-LVM (Traditional Partitions)**

```bash
┌─────────────────────────────────────────────────────────┐
│   🛠️  PARTITION RESIZING WORKSHOP 🛠️                   │
└─────────────────────────────────────────────────────────┘

# ⚠️ ALWAYS BACKUP FIRST! ⚠️
sudo rsync -avP /home /backup

# 🚢 METHOD A: GPARTED (GUI - EASIEST)
# 1. Boot from Arch USB
# 2. Run: sudo gparted
# 3. Select partition → Resize/Move
# 4. Apply changes

# 🏴‍☠️ METHOD B: PARTED (CLI)
sudo parted /dev/sda
  print                  # See current layout
  resizepart 2 100GB     # Resize partition 2 to 100GB
  quit
  
sudo resize2fs /dev/sda2 # Resize ext4 filesystem to fit

# ⚓ METHOD C: FDISK (Delete + Recreate)
sudo fdisk /dev/sda
  p                      # Print partitions
  d                      # Delete partition
  n                      # Create new (larger/smaller)
  w                      # Write changes
  
sudo resize2fs /dev/sda2 # Resize filesystem
```

```
    🌊 RESIZING VOYAGE VISUALIZATION 🌊
    
    BEFORE:
    ┌────────────────────────────────────────┐
    │ /boot │    /     │      /home          │
    │  1GB  │   50GB   │       449GB         │
    └────────────────────────────────────────┘
    
    SHRINK /home:
    ┌────────────────────────────────────────┐
    │ /boot │    /     │  /home  │ UNALLOC  │
    │  1GB  │   50GB   │  300GB  │  149GB   │
    └────────────────────────────────────────┘
    
    GROW / (root):
    ┌────────────────────────────────────────┐
    │ /boot │       /        │     /home     │
    │  1GB  │     199GB      │     300GB     │
    └────────────────────────────────────────┘
```

---

## 🧊 **METHOD 2: LVM (Logical Volume Manager)**

```bash
┌─────────────────────────────────────────────────────────┐
│       🎛️  LVM COMMAND CENTER 🎛️                        │
└─────────────────────────────────────────────────────────┘

# 🔍 CHECK IF YOU'RE USING LVM
lsblk | grep lvm
lvs                      # List logical volumes
vgs                      # List volume groups
pvs                      # List physical volumes

# 📈 INCREASE SIZE (Easy!)
sudo lvextend -L +20G /dev/vg0/root    # Add 20GB
sudo lvextend -l +100%FREE /dev/vg0/root  # Use all free space
sudo resize2fs /dev/vg0/root           # Resize ext4 filesystem

# 📉 DECREASE SIZE (Harder - filesystem first!)
sudo resize2fs /dev/vg0/home 100G      # Shrink filesystem
sudo lvreduce -L 100G /dev/vg0/home    # Then LV

# 🎯 ONE-LINER (Extend + Resize)
sudo lvextend -r -L +20G /dev/vg0/root
```

```
         LVM ARCHITECTURE (3-Layer Cake)
         
    ┌──────────────────────────────────────┐
    │  LOGICAL VOLUMES (What you see)     │ ← /dev/vg0/root
    │  ┌────────┬────────┬──────────┐     │   /dev/vg0/home
    │  │  root  │  home  │   swap   │     │
    │  └────────┴────────┴──────────┘     │
    ├──────────────────────────────────────┤
    │  VOLUME GROUP (Pool of space)       │ ← vg0
    │  ════════════════════════════════    │
    ├──────────────────────────────────────┤
    │  PHYSICAL VOLUMES (Actual disks)    │ ← /dev/sda2
    │  [SSD] [HDD] [NVMe]                 │   /dev/sdb1
    └──────────────────────────────────────┘
    
    🔑 KEY: LVM lets you resize easily!
```

---

# 🗄️ **PART 3: FILESYSTEM TYPES**

```bash
┌─────────────────────────────────────────────────────────┐
│       📂 FILESYSTEM FLEET COMPARISON 📂                 │
└─────────────────────────────────────────────────────────┘
```

```
         FILESYSTEM DECISION TREE
         
              START HERE
                 │
        Does data change often?
         ╱              ╲
       YES               NO
        │                │
    Need snapshots?   Fast reads?
     ╱        ╲         ╱      ╲
   YES        NO      YES      NO
    │          │       │        │
  BTRFS      EXT4     XFS     EXT2
  
  
  🏆 THE BIG 5 FILESYSTEMS:
  
  ┌──────────┬────────────┬───────────┬──────────────┐
  │   TYPE   │  USE CASE  │   SPEED   │   FEATURES   │
  ├──────────┼────────────┼───────────┼──────────────┤
  │  EXT4    │  General   │  ★★★★☆   │  Stable      │
  │          │  Purpose   │           │  Journaling  │
  ├──────────┼────────────┼───────────┼──────────────┤
  │  BTRFS   │  Modern    │  ★★★☆☆   │  Snapshots   │
  │          │  Desktop   │           │  Compression │
  ├──────────┼────────────┼───────────┼──────────────┤
  │  XFS     │  Large     │  ★★★★★   │  High perf   │
  │          │  Files     │           │  Scales huge │
  ├──────────┼────────────┼───────────┼──────────────┤
  │  F2FS    │  SSD/Flash │  ★★★★☆   │  Wear level  │
  │          │  Storage   │           │  Flash opt   │
  ├──────────┼────────────┼───────────┼──────────────┤
  │  ZFS     │  Servers   │  ★★★★☆   │  RAID, CoW   │
  │          │  NAS       │           │  Checksums   │
  └──────────┴────────────┴───────────┴──────────────┘
```

## 📚 **Filesystem Commands**

```bash
# 🔍 CHECK FILESYSTEM TYPE
lsblk -f
df -T
file -sL /dev/sda2

# 🏗️ CREATE FILESYSTEMS
sudo mkfs.ext4 /dev/sda2       # Ext4
sudo mkfs.btrfs /dev/sda3      # Btrfs
sudo mkfs.xfs /dev/sda4        # XFS
sudo mkfs.fat -F32 /dev/sda1   # FAT32 (for /boot)

# 🔧 FILESYSTEM OPERATIONS
# Ext4:
sudo tune2fs -l /dev/sda2      # Show info
sudo e2fsck -f /dev/sda2       # Check/repair (unmount first!)
sudo resize2fs /dev/sda2       # Resize

# Btrfs:
sudo btrfs filesystem show
sudo btrfs subvolume create /mnt/@home
sudo btrfs balance start /mnt

# XFS:
sudo xfs_info /dev/sda4
sudo xfs_repair /dev/sda4
sudo xfs_growfs /mnt           # Resize (mounted!)
```

```
    FILESYSTEM FEATURE COMPARISON
    
    ┌────────────────────────────────────────┐
    │  Feature      │ EXT4 │BTRFS│ XFS │ZFS │
    ├───────────────┼──────┼─────┼─────┼────┤
    │ Snapshots     │  ✗   │  ✓  │  ✗  │ ✓  │
    │ Compression   │  ✗   │  ✓  │  ✗  │ ✓  │
    │ Dedup         │  ✗   │  ✓  │  ✗  │ ✓  │
    │ Max File Size │ 16TB │ 16EB│ 8EB │256Q│
    │ Online Resize │  ✓   │  ✓  │  ✓  │ ✓  │
    │ Shrink        │  ✓   │  ✓  │  ✗  │ ✗  │
    │ RAID          │  ✗   │  ✓  │  ✗  │ ✓  │
    │ Stability     │  ✓✓  │  ✓  │  ✓✓ │ ✓  │
    └───────────────┴──────┴─────┴─────┴────┘
    
    Legend: EB=Exabyte, Q=Quettabyte
```

---

# 🚀 **PART 4: BOOTLOADERS (GRUB, SYSTEMD-BOOT, ETC)**

```bash
┌─────────────────────────────────────────────────────────┐
│        🎯 BOOTLOADER BATTLE STATION 🎯                  │
└─────────────────────────────────────────────────────────┘
```

## 🗺️ **Boot Process Map**

```
    THE BOOT VOYAGE (What happens when you press power)
    
    ⚡ POWER BUTTON PRESSED
         │
         ▼
    ┌────────────────────────┐
    │   FIRMWARE (BIOS/UEFI) │ ← Checks hardware
    └────────┬───────────────┘
             │ Reads boot device
             ▼
    ┌────────────────────────┐
    │   BOOTLOADER STAGE 1   │ ← MBR (446 bytes) or
    │   (MBR or EFI)         │   EFI partition
    └────────┬───────────────┘
             │ Loads main bootloader
             ▼
    ┌────────────────────────┐
    │   BOOTLOADER STAGE 2   │ ← GRUB/systemd-boot
    │   (GRUB menu)          │   Shows menu
    └────────┬───────────────┘
             │ User selects OS
             ▼
    ┌────────────────────────┐
    │   KERNEL (vmlinuz)     │ ← Linux kernel loads
    └────────┬───────────────┘
             │
             ▼
    ┌────────────────────────┐
    │   INITRAMFS (initrd)   │ ← Temporary root FS
    └────────┬───────────────┘
             │ Mounts real root
             ▼
    ┌────────────────────────┐
    │   INIT SYSTEM          │ ← systemd starts
    │   (systemd/OpenRC)     │
    └────────┬───────────────┘
             │
             ▼
    ┌────────────────────────┐
    │   LOGIN PROMPT         │ 🎉 YOU'RE IN!
    └────────────────────────┘
```

## ⚓ **Bootloader Comparison**

```bash
┌────────────────────────────────────────────────────────┐
│                 BOOTLOADER FLEET                       │
├────────────┬──────────┬────────────┬──────────────────┤
│ BOOTLOADER │ FIRMWARE │ COMPLEXITY │  BEST FOR        │
├────────────┼──────────┼────────────┼──────────────────┤
│ GRUB2      │ Both     │ High ★★★★☆│ Multi-boot, old  │
│            │ BIOS+UEFI│            │ hardware         │
├────────────┼──────────┼────────────┼──────────────────┤
│ systemd-   │ UEFI only│ Low  ★☆☆☆☆│ Simple, modern   │
│ boot       │          │            │ UEFI systems     │
├────────────┼──────────┼────────────┼──────────────────┤
│ rEFInd     │ UEFI only│ Med  ★★☆☆☆│ Mac, beautiful   │
│            │          │            │ GUI              │
├────────────┼──────────┼────────────┼──────────────────┤
│ SYSLINUX   │ BIOS     │ Low  ★★☆☆☆│ Legacy, USB boot │
├────────────┼──────────┼────────────┼──────────────────┤
│ LILO       │ BIOS     │ Low  ★☆☆☆☆│ Ancient (avoid!) │
└────────────┴──────────┴────────────┴──────────────────┘
```

## 🔧 **GRUB Commands**

```bash
# 📦 INSTALLATION
sudo grub-install /dev/sda              # BIOS
sudo grub-install --target=x86_64-efi \
  --efi-directory=/boot --bootloader-id=GRUB  # UEFI

# 🔄 UPDATE GRUB CONFIG
sudo grub-mkconfig -o /boot/grub/grub.cfg
sudo update-grub  # (on Debian/Ubuntu)

# 🎨 CONFIGURATION
sudo nano /etc/default/grub
  GRUB_TIMEOUT=5                 # Wait time
  GRUB_DEFAULT=0                 # Default entry
  GRUB_CMDLINE_LINUX_DEFAULT="quiet splash"
  GRUB_GFXMODE=1920x1080        # Resolution

# 🔍 TROUBLESHOOTING
sudo grub-probe /boot              # Check boot partition
sudo grub-install --recheck /dev/sda  # Reinstall

# 🏴‍☠️ GRUB RESCUE MODE
grub rescue> ls                    # List partitions
grub rescue> set root=(hd0,2)     # Set root
grub rescue> insmod normal
grub rescue> normal
```

```
    GRUB DIRECTORY STRUCTURE
    
    /boot/
    ├── grub/
    │   ├── grub.cfg          ← Main config (auto-generated)
    │   ├── grubenv           ← Environment vars
    │   ├── fonts/            ← Display fonts
    │   ├── locale/           ← Translations
    │   └── themes/           ← Custom themes
    │       └── mytheme/
    │           ├── theme.txt
    │           └── background.png
    ├── vmlinuz-linux         ← Kernel
    └── initramfs-linux.img   ← Initial RAM disk
    
    /etc/default/grub         ← Edit this for settings!
```

---

# 💧 **PART 5: SWAP SPACE**

```bash
┌─────────────────────────────────────────────────────────┐
│         🌊 SWAP OCEAN DEEP DIVE 🌊                      │
└─────────────────────────────────────────────────────────┘
```

```
    WHAT IS SWAP? (The Emergency Reservoir)
    
         RAM (Fast but limited)
    ┌──────────────────────────┐
    │ ████████████████░░░░░░░░ │ 75% full
    └────────┬─────────────────┘
             │ When RAM fills...
             ▼
    ┌──────────────────────────┐
    │  SWAP (Slow but infinite)│ ← Moves cold data here
    │  ▓▓▓▓░░░░░░░░░░░░░░░░░░░ │
    └──────────────────────────┘
         Disk space used as RAM overflow
         
    🔑 SWAP IS:
    - Virtual memory on disk
    - Emergency RAM overflow
    - Enables hibernation
    - ~10-30% slower than RAM
```

## ⚓ **Swap Size Recommendations**

```
    ┌─────────────┬─────────────┬──────────────┐
    │   RAM SIZE  │  SWAP SIZE  │  HIBERNATE?  │
    ├─────────────┼─────────────┼──────────────┤
    │   < 2GB     │   2x RAM    │   YES        │
    │   2-8GB     │   = RAM     │   YES        │
    │   8-64GB    │   0.5x RAM  │   OPTIONAL   │
    │   > 64GB    │   4-8GB     │   NO         │
    └─────────────┴─────────────┴──────────────┘
    
    Example: 16GB RAM = 8GB swap (or 16GB for hibernate)
```

## 🔧 **Swap Commands**

```bash
# 🔍 CHECK SWAP
swapon --show               # Current swap
free -h                     # RAM + swap usage
cat /proc/swaps             # Swap details

# 🏗️ CREATE SWAP FILE (Recommended for flexibility)
sudo dd if=/dev/zero of=/swapfile bs=1G count=8  # 8GB
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile

# Make permanent:
echo '/swapfile none swap defaults 0 0' | sudo tee -a /etc/fstab

# 🔨 CREATE SWAP PARTITION
sudo mkswap /dev/sda3       # Format partition as swap
sudo swapon /dev/sda3       # Activate
# Add to /etc/fstab:
# /dev/sda3 none swap defaults 0 0

# 🎛️ SWAP TUNING
sudo sysctl vm.swappiness=10     # Lower = less swap use (0-100)
sudo sysctl vm.vfs_cache_pressure=50

# Make permanent:
echo 'vm.swappiness=10' | sudo tee -a /etc/sysctl.conf

# ❌ REMOVE SWAP
sudo swapoff /swapfile      # Deactivate
sudo rm /swapfile           # Delete file
# Remove from /etc/fstab

# 📏 RESIZE SWAP FILE
sudo swapoff /swapfile
sudo dd if=/dev/zero of=/swapfile bs=1G count=16  # 16GB now
sudo mkswap /swapfile
sudo swapon /swapfile
```

```
    SWAP FILE vs SWAP PARTITION
    
    SWAP FILE:                  SWAP PARTITION:
    ✓ Easy to resize            ✓ Slightly faster
    ✓ No repartition needed     ✓ Works with old systems
    ✓ Modern standard           ✗ Fixed size
    ✗ Slightly slower           ✗ Needs repartition
    
    ┌────────────────┐          ┌────────────────┐
    │  /swapfile     │          │  /dev/sda3     │
    │  (Just a file) │          │  (Partition)   │
    └────────────────┘          └────────────────┘
         WINNER! ←                 Legacy
```

---

# 🧭 **PART 6: MASTER BOOT RECORD (MBR) vs GPT**

```bash
┌─────────────────────────────────────────────────────────┐
│        🗺️  PARTITION TABLE CARTOGRAPHY 🗺️               │
└─────────────────────────────────────────────────────────┘
```

```
    THE GREAT PARTITION TABLE WAR
    
    📜 MBR (1983 - Legacy)         🆕 GPT (2000s - Modern)
    ═══════════════════════       ═══════════════════════
    
    ┌────────────────────┐        ┌────────────────────┐
    │ LIMITATIONS:       │        │ ADVANTAGES:        │
    │ • 2TB max disk     │        │ • Unlimited size   │
    │ • 4 primary parts  │        │ • 128 partitions   │
    │ • No redundancy    │        │ • Backup table     │
    │ • BIOS only        │        │ • CRC checks       │
    └────────────────────┘        │ • UEFI required    │
                                  └────────────────────┘
    
    MBR STRUCTURE:              GPT STRUCTURE:
    ┌──────────────┐           ┌──────────────────┐
    │ Bootloader   │ 446 bytes│ Protective MBR   │
    ├──────────────┤           ├──────────────────┤
    │ Part Table   │  64 bytes│ Primary GPT Hdr  │
    ├──────────────┤           ├──────────────────┤
    │ Boot Sig     │   2 bytes│ Partition Array  │
    └──────────────┘           ├──────────────────┤
         512 bytes             │ Partitions...    │
                               ├──────────────────┤
                               │ Backup GPT Hdr   │
                               └──────────────────┘
                                   (End of disk)
```

## 🔍 **Check & Convert**

```bash
# 🔎 CHECK PARTITION TABLE TYPE
sudo fdisk -l /dev/sda | grep "Disklabel type"
sudo parted /dev/sda print | grep "Partition Table"
lsblk -d -o NAME,PTTYPE

# 🔄 CONVERT MBR TO GPT (⚠️ BACKUP FIRST!)
# Method 1: gdisk (safe, no data loss)
sudo gdisk /dev/sda
  r      # Recovery menu
  g      # Convert to GPT
  w      # Write and exit

# Method 2: sgdisk
sudo sgdisk -g /dev/sda

# ⚠️ AFTER CONVERSION: Reinstall bootloader!
sudo grub-install --target=x86_64-efi --efi-directory=/boot/efi \
  --bootloader-id=GRUB /dev/sda
```

---

# 📜 **MASTER CHEATSHEET**Now let me create a visual reference card with ASCII art:Now let me move these to the outputs directory and share them with you:# ⚓ **YOUR COMPLETE STORAGE ARSENAL IS READY!** ⚓

I've created two comprehensive guides for you:

## 📦 **What You Got:**

1. **🚢 Linux Storage Cheatsheet** - Complete command reference covering:
   - Every disk/space command you'll ever need
   - Step-by-step partition resizing (both traditional and LVM)
   - Filesystem comparison and operations
   - Swap management
   - GRUB bootloader commands
   - Arch-specific cleanup commands
   - Common troubleshooting scenarios

2. **🌊 Visual Reference Guide** - ASCII art diagrams explaining:
   - Complete boot process voyage (power → login)
   - Disk architecture (7 layers from hardware to user files)
   - Memory hierarchy (CPU cache → RAM → SWAP → Disk)
   - Partition scheme comparisons
   - Filesystem battle arena
   - Boot security flows (BIOS/MBR vs UEFI/GPT)
   - Troubleshooting decision trees

## 🎯 **Key Takeaways:**

**Filesystems:**
- **ext4** = Battle-tested, use for root `/`
- **btrfs** = Modern with snapshots, great for `/home`
- **xfs** = Speed demon for large files
- **f2fs** = Flash storage optimization

**Bootloaders:**
- **GRUB** = Most popular, works everywhere
- **systemd-boot** = Simple, UEFI-only
- Boot process: Firmware → MBR/EFI → GRUB → Kernel → Init

**Swap:**
- Swap File > Swap Partition (easier to resize)
- Size: 16GB RAM = 8GB swap (or 16GB for hibernation)
- Lower swappiness = better performance (`vm.swappiness=10`)

**Partition Tables:**
- **MBR** = Legacy (2TB limit, 4 partitions)
- **GPT** = Modern (unlimited, 128 partitions) ← Use this!

Happy sailing through the Linux storage seas! 🚢🌊